# Set to 'full-zk' when running against a real compact compile (no --skip-zk) output.
# Defaults to 'skip-zk' which shows the warning banner.
# See frontend/src/lib/buildMode.ts
VITE_BUILD_MODE=skip-zk
